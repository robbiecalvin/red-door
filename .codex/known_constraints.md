# Known Constraints

Frontend must remain static-host compatible.

Backend must operate as a standalone Node.js service.

Location privacy must prevent exact coordinate exposure.

Mode enforcement must always occur server-side.

Moderation systems must not be bypassable.